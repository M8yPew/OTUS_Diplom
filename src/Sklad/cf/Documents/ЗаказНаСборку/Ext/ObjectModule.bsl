#Область ОбработчикиСобытий

Процедура ОбработкаПроведения(Отказ, РежимПроведения)
		
КонецПроцедуры 

Процедура ПередЗаписью(Отказ, РежимЗаписи, РежимПроведения)
	
	Если Не Отказ Тогда
		ТекущийСтатус = СтатусыДокументовВызовСервера.СтатусЗаказаНаСборку(Ссылка);
		Если ТекущийСтатус = Перечисления.СтатусыЗаказовНаСборку.ПустаяСсылка() Тогда
			СтатусыДокументовВызовСервера.ОбновитьСтатусЗаказаНаСборку(Ссылка, "Создан");
		КонецЕсли;
	КонецЕсли;	
	
КонецПроцедуры

#КонецОбласти
