#Область ОбработчикиСобытий

Процедура ОбработкаПроведения(Отказ, РежимПроведения)
	
	Если Не Отказ Тогда
		ТекущийСтатус = СтатусыДокументовВызовСервера.СтатусЗаказаКлиента(Ссылка);
		Если ТекущийСтатус = Перечисления.СтатусыЗаказовКлиента.ПустаяСсылка() Тогда
			СтатусыДокументовВызовСервера.ОбновитьСтатусЗаказаКлиента(Ссылка, "Создан");
		КонецЕсли;
	КонецЕсли;
	
КонецПроцедуры 

#КонецОбласти
